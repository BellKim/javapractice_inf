<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>Insert title here</title>
		<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
		<script type="text/javascript">
			$(document).ready(function(){//022번 참조. 
				var arr=[10,20,30,40];
/* 				for(var i in arr)
					document.write(arr
							[i]+"<br/>"); */
			//변수에 저장된 값을 가져와서 처리한다. 
			$(arr).each(function(index, value){
				//document.write(arr[i]+"<br/>");
/* 				0 , 10
				1 , 20
				2 , 30
				3 , 40 */
				//document.write(index + " , " + value+"<br/>");
			});
/* 			$.each(arr,function(index, value){
 			//$(arr).each(function(index, value){ //바로 위와 아래가 똑같은 문장이다. 
				document.write(index + " : ");
				document.write(value+"<br/>");
			}); */
			
/* 			$(arr).each(function(index, value){
				document.write(index+"<br/>");
			});	*/			
			
			var size=$('ul li');
			/* console.log(size.lengt);
			//0
			alert(size.lengt); */
			$('ul li').each(function(index, value){
				//document.write(index + " , " + value + "<br/>");
				/* 	0 , [object HTMLLIElement]
					1 , [object HTMLLIElement]
					2 , [object HTMLLIElement]
					3 , [object HTMLLIElement] htmlelement 로 나오면 기본적으로 dom,   object 로 나오면 jquery 이다.  */
					
/* 			document.write("<br/>");
			document.write(index + " , " + value.nodeName + "<br/>");
			//document.write(index + " , " + value.text() + "<br/>");//오류. DOM에서 제공안됨. value는 dom객체타입에서 가져왔다. 
			document.write(index + " , " + value.childNodes[0].nodeValue+ "<br/>");
			document.write(index + " , " + $(value).text() + "<br/>");//(value.text().innerText) 에 괄호를 싸주는 이유는 innerText는 javasctipt 이기 때문에 jquery와 혼동이옴.
			document.write("<br/>"); */
			
			});
			
			
			//$.each($('ul li'),function(index,value){
			$.each(size,function(index,value){
				document.write(index + ",  ,,,,,,,,,,,, " + value.nodeName + "<br/>");
			
			}); 
		});
			
		
		</script>
	</head>
	<body>
		<ul>
			<li> content1 </li>
			<li> content2 </li>
			<li> content3 </li>
			<li> content4 </li>
		</ul>
	
	</body>
</html>